import 'package:flutter/material.dart';

class VenderScreen extends StatelessWidget {
  static const String id = '\vender-Screen';

  const VenderScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Vender Screen"),
    );
  }
}
